# products/urls.py
from django.urls import path
from . import views

urlpatterns = [
    # Define your product-related routes here
    # Example: path('list/', views.ProductListView.as_view(), name='product-list'),
]
